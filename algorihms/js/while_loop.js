function WhileLoopFunction()
{
    var number = 1

    while(number < 11)
    {
        //display a message
        document.write("The number is ", number, "<br>")

        //increment number
        number = number + 1;
    }
}